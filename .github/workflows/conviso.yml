name: Conviso
on:
  pull_request:
  push:
    branches: [ gh-pages ]
jobs:
  conviso:
    name: Conviso [SI]
    uses: vindi/conviso-settings/.github/workflows/api-docs.yml@main
    secrets:
      CONVISO_API_KEY: ${{ secrets.CONVISO_API_KEY }}
